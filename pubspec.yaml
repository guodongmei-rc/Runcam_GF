name: runcam_gf
description: "RuncamGF — Gyroflow 视频防抖原生集成(iOS/Android)Flutter 插件。"
version: 0.2.0
homepage: https://gyroflow.xyz
# 私有分发(path/git 依赖):编辑器吐司用了 toastification 的 git fork,
# 故不发布到 pub.dev。宿主以 path 或 git 依赖本插件。
publish_to: 'none'

environment:
  sdk: ^3.11.1
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  # 共享编辑器 UI(lib/src/ui)随插件发布所需的运行时依赖。
  flutter_localizations:
    sdk: flutter
  intl: any
  flutter_svg: ^2.0.10
  shared_preferences: ^2.5.5
  # 吐司
  toastification:
    git:
      url: https://github.com/guodongmei-rc/toastification.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # 阶段2:统一 Dart↔native 引擎桥(强类型代码生成)。
  # 改 pigeons/runcam_gf_api.dart 后跑: dart run pigeon --input pigeons/runcam_gf_api.dart
  pigeon: ^22.6.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  # 生成本地化代码(读取 lib/src/ui/l10n/*.arb,见 l10n.yaml)。
  generate: true

  # 编辑器 UI 图标(以 package 资源形式随插件发布:packages/runcam_gf/assets/icons/...)。
  assets:
    - assets/icons/

  plugin:
    platforms:
      android:
        package: com.runcam.runcam_gf
        pluginClass: RuncamGfPlugin
      ios:
        pluginClass: RuncamGfPlugin

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
